spring:
  application:
    name: myWeb
  profiles:
    active:
    - dev

zuul:
  #prefix: /mySpringCloud
  routes:
    myService1route:
      serviceId: myService1
      path: /myService1Url/**

---
spring:
  profiles: dev
    
server:
  port: 8081
  
eureka:
  instance:
    instance-id: myWeb_dev_instance
    prefer-ip-address: true     #访问路径可以显示IP地址 
    #ip-address: localhost
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka/      

info: 
  app.name: myWeb-springCloud
  company.name: www.surichard.com
  build.artifactId: $project.artifactId$
  build.version: $project.version$

---
spring:
  profiles: test
    
server:
  port: 8091
  
eureka:
  instance:
    instance-id: myWeb_test_instance
    prefer-ip-address: true     #访问路径可以显示IP地址 
    #ip-address: localhost
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka/      

info: 
  app.name: myWeb-springCloud
  company.name: www.surichard.com
  build.artifactId: artifactIdTest
  build.version: versionTest
